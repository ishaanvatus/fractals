#ifndef _fractals_h
#define _fractals_h

#include "scrimage.h"
#include "fractals.h"
#include <stdio.h>

void sierpinski_triangle(Image image, int order, point p0, point p1, point p2, pixel color);

#endif
